---
title: "Treningsoversikt"
format: html
editor_options: 
  chunk_output_type: console
---

```{r}
# Last inn nødvendige biblioteker
library(readxl)
library(ggplot2)
library(tidyverse)
library(gt)

# Les data fra Excel
treningsdata <- read_excel("treningsdata.xlsx")

treningsdata |> 
        print()
ggplot(treningsdata, aes(x = uke, y = "totalt_timer_utholdenhet")) +
  geom_bar(stat = "identity") +
  labs(title = "Treningsoversikt", x = "Dato", y = "Varighet (min)") +
  theme_minimal()

```